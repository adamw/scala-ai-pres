package pres

object S150_Cellar:
  val _1 = "A set of tools to interface with Java dependencies"
  val tools = List(
    "get",
    "get-external",
    "list",
    "list-external",
    "search",
    "search-external",
    "deps",
    "get-source"
  )

  // /plugin marketplace add virtuslab/cellar
  // /plugin install cellar@virtuslab-cellar

  // Fetch the source of Ox's Fork
