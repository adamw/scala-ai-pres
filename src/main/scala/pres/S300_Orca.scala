package pres

class S300_Orca:
  /** Deterministic, AI-driven development flows. */

  val _1 = "Type-safe API to describe development flows"
  val _2 = "Plan-implement-review loop from which an agent cannot deviate"
  val _3 = "Run from scala-cli, CLI or via a skill"

  // For a version oriented towards teams, regulated environments, enterprises
  // -> Visdom

  val features = List(
    "resumable flows",
    "any language / ecosystem / framework",
    "reusable flows, commitable configuration",
    "code with claude, review with codex",
    "let agents write flows on-demand"
  )
